export enum View {
  grid = "grid",
  list = "list",
}

export enum Color {
  black = "black",
  color = "color",
  all = "all",
}

export enum Sort {
  added = "added",
  artist = "artist",
  title = "title",
}

export enum SortOrder {
  asc = "asc",
  desc = "desc",
}
